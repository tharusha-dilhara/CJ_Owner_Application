import 'package:flutter/material.dart';


class DailyreportsView extends StatelessWidget {
  const DailyreportsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Daily reports View")),
    );
  }
}